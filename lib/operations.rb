def unsafe?(speed)

  if speed > 60
      "unsafe"
  else
      "safe"
    
end

def unsafe?(speed)
    
    if speed < 40
        "unsafe"
        
    else "safe"    

def not_safe?(speed)
	
	speed < 40 ? "unsafe" : "safe" 
end
	













    